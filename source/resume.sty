\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{resume}[2026/02/07 Resume style]

% Layout
\usepackage[letterpaper,margin=0.75in]{geometry}
\setlength{\parindent}{0pt}
\setlength{\parskip}{2pt}

% Typography
\usepackage[T1]{fontenc}
\usepackage{lmodern} % safe default everywhere
\usepackage{microtype}
\sloppy
\emergencystretch=2em

% Links / PDF metadata
\usepackage[hidelinks]{hyperref}
\hypersetup{
  pdftitle={Colbyn Wadman - Resume},
  pdfauthor={Colbyn Wadman}
}

% Images
\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{calc}
\usepackage{caption}
\captionsetup[figure]{labelformat=empty}


% Lists
\usepackage{enumitem}
% \setlist[itemize]{leftmargin=*, topsep=2pt, itemsep=1pt, parsep=0pt}
\setlist[itemize]{leftmargin=*, topsep=1pt, itemsep=1pt, parsep=0pt, partopsep=0pt}

% Section formatting
\usepackage{titlesec}
\titleformat{\section}{\large\bfseries}{}{0pt}{}[\titlerule]
\titlespacing*{\section}{0pt}{10pt}{6pt}

% Simple “entry” macros
% \newcommand{\entry}[4]{%
%   \textbf{#1}\hfill #2\\%
%   \textit{#3}\hfill \textit{#4}\\[-2pt]%
% }
\newcommand{\entry}[4]{%
  \textbf{#1}%
  \if\relax\detokenize{#2}\relax%
  \else\hfill #2%
  \fi\\%
  \textit{#3}\hfill \textit{#4}\\[-2pt]%
}


% \newcommand{\project}[3]{%
%   \textbf{#1} \hfill \href{#2}{#2}\\[-2pt]%
%   #3\\%
% }
% \newcommand{\project}[3]{%
%   \textbf{#1}%
%   \if\relax\detokenize{#2}\relax\hfill%
%   \else\hfill \href{#2}{#2}%
%   \fi\\[-2pt]%
%   #3\vspace{2pt}%
% }
\newcommand{\project}[3]{%
  \textbf{#1}%
  \if\relax\detokenize{#2}\relax%
  \else\hfill \href{#2}{#2}%
  \fi\\[-2pt]%
  #3\vspace{2pt}%
}


% \printlink{url} -> prints url, links it
\newcommand{\printlink}[1]{\href{#1}{#1}}



\newcommand{\tightitem}{\itemsep1pt\parskip0pt\parsep0pt}

\newenvironment{tightitemize}{%
  \begin{itemize}\tightitem
}{%
  \end{itemize}
}
